apiVersion: v1
kind: Service
metadata:
  name: nodejs-deployment 
  namespace: default
spec:
  selector:
    app: nodejs-app
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30090
