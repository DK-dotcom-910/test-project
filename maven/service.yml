apiVersion: v1
kind: Service
metadata:
  name: maven-app-service
spec:
  selector:
    app: maven
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
