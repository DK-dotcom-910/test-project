apiVersion: v1
kind: Service
metadata:
  name: maven-app-service
  namespace: default
spec:
  selector:
    app: maven-app
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
